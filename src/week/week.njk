---
pagination:
  data: weeks
  size: 1
  alias: weekData
permalink: "/week/{{ weekData.week }}/index.html"
layout: layout.njk
rootPath: "../../"
eleventyComputed:
  title: "Week {{ weekData.week }} mods | Project Daedalus"
  description: "{{ weekData.count }} Icarus mod(s) compatible with game week {{ weekData.week }}."
---
<section class="max-w-5xl mx-auto px-4 py-6 md:py-10">
  <div class="flex items-center justify-between flex-wrap gap-4 mb-6">
    <div>
      <h1 class="text-3xl md:text-4xl font-extrabold text-icarus-500">Game week {{ weekData.week }}</h1>
      <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">{{ weekData.count }} mod{% if weekData.count != 1 %}s{% endif %} compatible with this week</p>
    </div>
    <a href="{{ rootPath }}stats/" class="text-sm text-icarus-500 hover:underline">← all weeks</a>
  </div>

  <div class="grid grid-cols-1 gap-3 md:grid-cols-2">
    {% for m in weekData.mods %}
      <a href="{{ rootPath }}mods/{{ m.author_slug }}/{{ m.slug }}/" class="block p-4 border rounded-lg border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800/50 hover:bg-slate-50 dark:hover:bg-slate-800 no-underline">
        <div class="flex items-start justify-between gap-3 mb-1">
          <div class="font-semibold text-slate-800 dark:text-slate-200">{{ m.name }}</div>
          <div class="flex gap-1 flex-shrink-0">
            {% for t in m.file_types %}
              <span class="inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-semibold uppercase
                {% if t == 'exmodz' %}bg-icarus-500/20 text-icarus-500
                {% elif t == 'pak' %}bg-emerald-500/20 text-emerald-600
                {% else %}bg-slate-500/20 text-slate-600{% endif %}">{{ t }}</span>
            {% endfor %}
          </div>
        </div>
        <div class="flex items-center gap-2 mb-2 text-xs text-slate-500 dark:text-slate-400">
          <span>by <a href="{{ rootPath }}authors/{{ m.author_slug }}/" class="hover:text-icarus-500" onclick="event.stopPropagation()">{{ m.author }}</a></span>
          {% if m.version %}<span>· v{{ m.version }}</span>{% endif %}
        </div>
        {% if m.description %}<p class="text-sm text-slate-600 dark:text-slate-400 line-clamp-2">{{ m.description | truncate(160) }}</p>{% endif %}
      </a>
    {% endfor %}
  </div>
</section>
